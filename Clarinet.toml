[project]
name = "async-voting-simulator"
authors = ["async-voting-developer"]
description = "A decentralized voting simulation platform on the Stacks blockchain"
telemetry = false
requirements = []
[contracts.async-voting-simulator]
path = "contracts/async-voting-simulator.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
